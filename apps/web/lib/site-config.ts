/**
 * Placeholder until the real production domain is known — swap via
 * NEXT_PUBLIC_SITE_URL once Jufoods has a confirmed domain.
 */
export const SITE_URL = process.env.NEXT_PUBLIC_SITE_URL ?? 'https://jufoods.com'
